# Minimal ProGuard rules (shrinking is disabled in release; file kept for future use)
# If you enable shrinking, avoid keeping Play Core dependent classes unless you add Play Core deps.
# -dontwarn com.google.android.play.**